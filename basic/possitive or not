#include <stdio.h>

int main() {
    int n;

    printf("Enter a number: ");
    scanf("%d", &n);
    if (n>0)
        printf("Possitive number");
    else if (n<0)
        printf("negative number");
    else
        printf("zero number");
    return 0;
    
}

