#include <stdio.h>
int main() {
    int i, j;

    for (i = 1; i <= 3; i++) {       // Outer loop
        for (j = 1; j <= 2; j++) {   // Inner loop
            printf("i=%d j=%d\n", i, j);
        }
    }

    return 0;
}
