#include<stdio.h>
int main() {
    int salary;
    printf("Enter your salary:");
    scanf("%d",&salary);
    if (salary>30000) {
        printf("Bonus Eligible");
    }
    else if (salary<30000){
    printf("Not eligible");
    }
    else{
    printf("Exactly not eligible");
    }
}
