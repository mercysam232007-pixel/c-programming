#include<stdio.h>
int main() {
    int balance;
    printf("Enter your balance: ");
    scanf("%d",&balance);
    if (balance<1000)
        printf("Low balance");
    else
        printf("Sufficient balance");
}
