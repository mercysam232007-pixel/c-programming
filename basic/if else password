#include <stdio.h>
int main() {
    int password;
    printf("enter your password");
    scanf("%d",&password);
    if (password>=8)
        printf("Strong password");
    else if (password<8)
        printf("Weak password");
    else if (password==8)
        printf("meets minimum requirements");
}
