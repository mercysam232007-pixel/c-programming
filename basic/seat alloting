#include <stdio.h>

int main() {
    int seat;

    printf("Enter your seat number: ");
    scanf("%d", &seat);
    if (seat==1&&seat==5)
        printf("alloted");
    else
        printf("not alloted");
}

