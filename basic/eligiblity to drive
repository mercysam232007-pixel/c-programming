#include<stdio.h>
int main() {
    int age;
    printf("Enter your age: ");
    scanf("%d",&age);
    if (age>=18&&age<=70)
        printf("Eligible to drive");
    else
        printf("Not eligible to drive");
}
