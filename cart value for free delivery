#include <stdio.h>

int main() {
    int cart_value;

    printf("Enter cart value: ");
    scanf("%d", &cart_value);

    if (cart_value >= 499)
        printf("Free delivery");
    else
        printf("Delivery charge applies");

    return 0;
}
