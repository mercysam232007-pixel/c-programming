#include <stdio.h>

int main()
{
    int i, j, num = 1;

    for(i = 1; i <= 4; i++)      // rows
    {
        for(j = 1; j <= i; j++)  // columns
        {
            printf("%d ", num);
            num++;
        }
        printf("\n");
    }

    return 0;
}
