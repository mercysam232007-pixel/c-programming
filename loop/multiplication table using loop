#include<stdio.h>
#include <stdio.h>

int main() {
    int i,j,n;
    printf("Enter a number: ");
    scanf("%d",&n);
    for (i=1; i<=n; i++){
        printf("Multiplication table of %d ",i);
        for (j=1; j<=10; j++) {
            printf("%d * %d = %d\n",i,j,i*j);
        }
    }

}
