#include<stdio.h>
int main() {
    int i,n,count=0;
    printf("Enter the numbers to be count: ");
    scanf("%d",&n);
    for (;n!=0;n=n/10) {
        count++;
    }
    printf("%d",count);
return 0;
}
